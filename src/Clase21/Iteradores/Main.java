package Clase21.Iteradores;

public class Main {
    public static void main(String[] args) {
        /*
        Los iteradores recorren una colección de objetos elemento a elemento.
        Para recorrer la coleccion debemos implementar la interfaz Iterator, que recibe un genérico.
        Construye un objeto iterador.
        Al comenzar nos encontramos al principio de la colección, next() salta al siguiente elemento de la colección.
        A partir de ahí analiza el objeto saltado.
        next() lanza una excepción si ya no hay mas elementos en la colección.
        hasnext() devuelve true si hay un elemento siguiente en la colección.
        remove() elimina un objeto.
        Hay operaciones que son mucho más sencillas de recorrer con un Iterator.
         */
    }
}
